# 健康数据可视化应用

基于 Next.js 14 的个人健康数据可视化系统，集成 Claude Code SDK 提供智能健康分析。

## 功能特性

- 📊 **交互式仪表盘** - 综合展示所有健康数据
- 🤖 **AI 健康分析** - 基于 Claude 的对话式数据分析
- 📑 **报告生成** - 自动生成 HTML 健康报告
- 📁 **本地数据** - 直接读取 JSON 文件，无需数据库
- 🎨 **医疗主题** - 复用现有报告的视觉风格

## 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

编辑 `.env.local` 文件，添加你的 Anthropic API Key：

```env
ANTHROPIC_API_KEY=sk-ant-xxxxx
```

获取 API Key: https://console.anthropic.com/

### 3. 启动开发服务器

```bash
npm run dev
```

应用将在 http://localhost:3000 启动

### 4. 构建生产版本

```bash
npm run build
npm start
```

## 技术栈

- **框架**: Next.js 14 (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS 3.4
- **图表**: ECharts 5.5
- **AI SDK**: @anthropic-ai/sdk
- **图标**: Lucide React

## 项目结构

```
nextjs-app/
├── src/
│   ├── app/              # Next.js App Router 页面
│   │   ├── page.tsx      # 主页（仪表盘）
│   │   ├── layout.tsx    # 根布局
│   │   └── globals.css   # 全局样式
│   ├── components/       # React 组件
│   │   ├── charts/       # ECharts 图表组件
│   │   ├── dashboard/    # 仪表盘组件
│   │   └── analysis/     # 分析组件
│   ├── lib/              # 工具库
│   │   ├── data-reader.ts      # 数据读取器
│   │   ├── anthropic.ts        # Claude SDK 封装
│   │   └── report-generator.ts # 报告生成器
│   ├── types/            # TypeScript 类型定义
│   └── styles/           # 样式文件
├── package.json
├── tsconfig.json
├── tailwind.config.ts
└── next.config.js
```

## 数据源

应用读取 `../data-example/` 目录中的 JSON 文件：

- `profile.json` - 用户档案
- `allergies.json` - 过敏记录
- `cycle-tracker.json` - 月经周期追踪
- `pregnancy-tracker.json` - 孕期记录
- `menopause-tracker.json` - 更年期记录
- `screening-tracker.json` - 筛查记录
- `vaccinations.json` - 疫苗接种
- `radiation-records.json` - 辐射剂量
- `interactions/interaction-db.json` - 药物相互作用

## 输出目录

所有生成的报告和导出的图表保存在 `../history-chart/` 目录：

- `generated-reports/` - HTML 健康报告
- `charts/` - 导出的图表（PNG）

## 开发状态

### ✅ 已完成
- [x] 项目初始化和配置
- [x] TypeScript 类型定义
- [x] 数据读取层
- [x] 基础仪表盘页面
- [x] 全局样式和医疗主题

### 🚧 开发中
- [ ] ECharts 图表组件（体重、化验结果、症状、药物）
- [ ] Claude SDK 集成
- [ ] AI 对话界面
- [ ] 报告生成器
- [ ] 图表导出功能
- [ ] 深度分析页面（女性健康、慢性病、预防保健）

### 📋 计划中
- [ ] 实时数据更新
- [ ] 数据筛选和搜索
- [ ] 响应式优化
- [ ] 性能优化
- [ ] 用户偏好设置
- [ ] 离线支持（PWA）

## 与现有系统集成

本应用设计为与现有的 Python 脚本和平共存：

- ✅ 不修改现有数据结构
- ✅ 不干扰 Python 报告生成
- ✅ 复用现有样式和图表配置
- ✅ 集成现有思考库（thinking-libraries）

## 安全与隐私

- ✅ 所有数据保持本地
- ✅ API Key 仅存储在 `.env.local`（不提交到 Git）
- ✅ 仅在用户主动查询时调用 Claude API
- ✅ 不向外部服务发送敏感健康数据

## 故障排除

### 构建错误

如果遇到构建错误，尝试：

```bash
rm -rf .next node_modules
npm install
npm run build
```

### 数据读取失败

确保 `data-example/` 目录在正确位置：

```bash
# 应该存在
../data-example/profile.json
../data-example/allergies.json
# ...
```

### API Key 错误

确保 `.env.local` 包含有效的 API Key：

```env
ANTHROPIC_API_KEY=sk-ant-xxxxx
```

## 贡献

欢迎提交 Issue 和 Pull Request！

## 许可证

MIT License - 详见主项目 LICENSE 文件

## 联系方式

- 项目主页: https://github.com/huifer/Claude-Ally-Health
- 公司: WellAlly Tech (https://www.wellally.tech/)
